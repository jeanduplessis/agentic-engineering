{
  "evals": [
    {
      "id": "recovery-contract-from-session-feedback",
      "prompt": "Use the epic-implement skill to describe how you would improve or resume an interrupted /epic-implement run. Scenario: an implementation gate executor closed task ait-1az.2 before /task-validate failed; a generated helper script later crashed on unsupported mapfile; validation found a missed parent-update path that can invalidate child records; no Pi executable or native subagent is available, but the current session can perform gates; and commands/tdd-task.md exists alongside an optional .pi/prompts/tdd-task.md. The run may need to resume. Do not execute commands. Return the controller rules and next safe steps, including gate execution fallback and template precedence.",
      "checks": [
        {
          "id": "non-empty-response",
          "type": "non_empty_response"
        }
      ]
    }
  ]
}
