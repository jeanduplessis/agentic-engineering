{
  "schema_version": 1,
  "skill": {
    "name": "code-review-workflow",
    "path": "../SKILL.md"
  },
  "suites": [
    {
      "name": "workflow",
      "type": "workflow",
      "mode": "forced",
      "fixture": {
        "type": "empty"
      },
      "cases": [
        {
          "id": "logic-review-evidence-trace-impact",
          "prompt": "You are acting as @review-logic for a code-review-workflow smoke test. Use only the inline resolved scope and diff below; do not read files, write files, or run shell commands. Load the review workflow, then return the normal logic review output with exact labels `## Review Summary (Logic & Error Handling)`, `**Evidence:**`, `**Trace:**`, and `**Impact:**`.\n\n## Resolved Review Scope\n\n**Base ref:** origin/main\n**Scope note:** Inline fixture for one modified file.\n\n| File | Status | Sources |\n|------|--------|---------|\n| src/cart.ts | modified | branch |\n\n```diff\ndiff --git a/src/cart.ts b/src/cart.ts\n@@\n export function totalAfterCoupon(totalCents: number, coupon?: Coupon): number {\n-  if (!coupon) return totalCents;\n+  if (!coupon) return 0;\n   if (coupon.type === 'percent') {\n     return Math.round(totalCents * (1 - coupon.value));\n   }\n   return Math.max(0, totalCents - coupon.value);\n }\n```\n\nReview only the changed line for logic and error-handling issues. Report the missing-coupon behavior regression as a WARNING on `src/cart.ts`.",
          "checks": [
            {
              "id": "summary-heading",
              "type": "required_content",
              "value": "## Review Summary (Logic & Error Handling)"
            },
            {
              "id": "warning-severity",
              "type": "required_content",
              "value": "**[WARNING]**"
            },
            {
              "id": "file-mentioned",
              "type": "required_content",
              "value": "src/cart.ts"
            },
            {
              "id": "evidence-label",
              "type": "required_content",
              "value": "**Evidence:**"
            },
            {
              "id": "trace-label",
              "type": "required_content",
              "value": "**Trace:**"
            },
            {
              "id": "impact-label",
              "type": "required_content",
              "value": "**Impact:**"
            },
            {
              "id": "no-clean-review",
              "type": "forbidden_content",
              "value": "No issues found"
            }
          ]
        }
      ]
    }
  ],
  "configurations": {
    "with_skill": {
      "harness": "pi",
      "force_skill": true,
      "timeout_seconds": 180
    },
    "without_skill": {
      "harness": "pi",
      "force_skill": false,
      "timeout_seconds": 180
    }
  }
}
