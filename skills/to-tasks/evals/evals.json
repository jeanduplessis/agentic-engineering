{
  "evals": [
    {
      "id": "draft-vertical-slice-breakdown",
      "prompt": "Use the to-tasks workflow on this implementation brief. You are in an empty sandbox with no initialized beads database; do not initialize beads. Draft the implementation task set you would create, include the task body fields, and ask how I want to proceed with beads setup instead of claiming tasks were created. Brief: Add saved search alerts to an existing issue tracker. Users can save a query, choose daily or immediate email alerts, receive notifications when new matching issues appear, and disable alerts from settings. Constraints: reuse the existing issue search UI and email job framework; include a design-review checkpoint only if needed before implementation.",
      "checks": [
        {
          "id": "non-empty-response",
          "type": "non_empty_response"
        }
      ]
    }
  ]
}
